# api_data_fetch.py
This project demonstrates how to consume a public REST API using Python. The program fetches user data from a free public API based on user input, parses the JSON response, displays formatted output, and stores the data locally in a JSON file.
